Please take [todo](https://github.com/mitsuhiko/agent-stuff/blob/main/extensions/todos.ts) and wrap a agent friendly CLI around 
it that you would like to use.  Leave the Pi UI in place, so that this can be used by an agent or a human via the CLI or as a 
CLI plugin.

Eventually, I'd like to expand this to include a memory facilty like beads has, hence the name pearls, but not now.  For now, I 
just want todo.ts but with a CLI and we'll see where we go from there.

Use todo.ts for your planning.
